name: Lint code

on:
  push:
    branches:
      - main  

jobs:
  lint:
    name: Lint code
    runs-on: ubuntu-latest
    
    steps: 
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Test lint
        run: |
          npm install
          npm run lint

